/**
 * Les classes contenues dans ce package sont
 * utilisées pour les positions des entités sur la carte
 * en jeu (en fonction du scale) et sur la grille {@link config.MazeConfig}.
 */
package geometry;
